# OpenFreeMap Docker Configuration

# Area to download
# Options: 'planet' (full world, ~100GB) or 'monaco' (test data, ~6MB)
AREA=monaco

# Version to download
# Use 'latest' for most recent, or specify exact version like '20240101_120000_pt'
VERSION=latest

# Skip flags for faster iteration during development
SKIP_DOWNLOAD=false
SKIP_EXTRACT=false
SKIP_ASSETS=false

# Nginx configuration
# Set to your domain name for production
NGINX_HOST=localhost
HTTP_PORT=8080

# Data path - where all data will be stored
# This should be an absolute path with sufficient disk space
# Minimum: 10GB for Monaco, 300GB for Planet
DATA_PATH=/path/to/data

# CDN URLs (optional - defaults to official OpenFreeMap CDN)
# Customize if using your own CDN or mirror
# BTRFS_CDN_URL=https://btrfs.openfreemap.com
# ASSETS_CDN_URL=https://assets.openfreemap.com

